room_manager_server:
  host:
  port:
  protocol:
  path:
  version:

exchange_manager_server:
  host:
  port:
  protocol:
  path:
  version:

data_base_server:
  room_manager:
    host:
    port:
    db_name:
  exchange:
    host:
    port:
    db_name:
